<resources>

    <!-- Base application theme. -->
    <!-- epidi xrisimopoioume to neo materialDesign 2.0 -->
    <!-- to paren theme tou app den tha einai pia AppCompat alla materiaComponents -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- kanoume remove tin default action bar afou exoume tin custom toolbar mas -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- edo orizoume to theme tis toolbar mas -->
    <style name="ToolbarTheme" parent="ThemeOverlay.MaterialComponents.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">#EEEEEE</item>
    </style>

</resources>
